services:
  - type: web
    name: moodsticker-y2k
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    healthCheckPath: /views/landing.html

